apiVersion: apps/v1

kind: Deployment
metadata:
  name: analyze-service
  labels:
    app: analyze-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analyze-service
  template:
    metadata:
      labels:
        app: analyze-service
    spec:
      containers:
        - name: analyze-service
          image: analyze-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: url
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"

---
apiVersion: v1
kind: Service
metadata:
  name: analyze-service
spec:
  selector:
    app: analyze-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085