spring:
  application:
    name: book-service
  config:
    import: configserver:http://localhost:8071
  cloud:
    config:
      uri: http://localhost:8071
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1000
        multiplier: 1.1